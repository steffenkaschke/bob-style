export enum TableType {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum SortDirections {
  Asc = 'asc',
  Desc = 'desc'
}

export enum RowSelection {
  Multiple = 'multiple',
  Single = 'single',
}

export enum PinDirection {
  Left = 'left',
  Right = 'right',
}
