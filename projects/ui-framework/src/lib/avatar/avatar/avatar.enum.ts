export enum AvatarSize {
  mini = 36,
  small = 60,
  medium = 90,
  large = 120,
  xlarge = 160,
}

export enum AvatarOrientation {
  horizontal = 'horizontal',
  vertical = 'vertical',
}

export enum AvatarBadge {
  approved = 'approved',
  pending = 'pending',
  rejected = 'rejected',
  online = 'online',
  offline = 'offline',
  error = 'error',
}
