import {CapsuleBlot} from './formats/capsule-blot';

export enum FormatTypes {
  bold = 'bold',
  italic = 'italic',
  underline = 'underline',
  link = 'link',
  size = 'size',
}

export enum BlotType {
  Link = 'Link',
  Capsule = 'Capsule',
}
