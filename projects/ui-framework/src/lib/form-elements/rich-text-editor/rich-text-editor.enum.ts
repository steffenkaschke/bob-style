export enum RTEType {
  primary = 'primary',
  secondary = 'secondary'
}

export enum FormatTypes {
  bold = 'bold',
  italic = 'italic',
  underline = 'underline',
  link = 'link',
  size = 'size',
}

export enum BlotType {
  Link = 'Link',
}
