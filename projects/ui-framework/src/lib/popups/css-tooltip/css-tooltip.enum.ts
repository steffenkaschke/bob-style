export enum CSSTooltipWrap {
  normal = 'normal',
  nowrap = 'nowrap',
  pre = 'pre'
}

export enum CSSTooltipPosition {
  above = 'above',
  below = 'below'
}

export enum CSSTooltipShowOn {
  focus = 'focus',
  hover = 'hover'
}

export enum CSSTooltipTextAlign {
  left = 'left',
  right = 'right',
  center = 'center'
}
